# Variáveis de ambiente para o Admin (Next.js)
# Copie para .env e ajuste os valores

# URL da API backend (incluindo /api/v1)
NEXT_PUBLIC_API_URL=https://api.seudominio.com.br/api/v1

# Nome exibido na interface
NEXT_PUBLIC_APP_NAME=Store SaaS Admin
