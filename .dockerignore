# Contexto de build do admin (docker-compose.admin.yml usa a raiz como contexto).
# Mantém o contexto enxuto e evita vazar artefatos pesados/irrelevantes.

**/node_modules
**/.next
**/.turbo
.git
.gitignore

# Backend e infra não fazem parte da imagem do admin
backend
docker

# PDV: só o package.json é necessário (copiado explicitamente no Dockerfile)
pdv/src-tauri
pdv/dist
pdv/node_modules

# Diversos
**/*.log
**/.env.local
**/.DS_Store
docs
.claude
